# Controlled checkpoint 01: begin repeated families and paths

- checkpoint_schema_version: 1
- checkpoint_id: begin_repeated_families_and_paths
- control_only: true
- must_not_enter_generation_spec: true

This checkpoint contains no source facts, business semantics, defaults, field values, or output content. Do not copy,
summarize, encode, cite, or otherwise place this file, its path, its id, or its instructions in the generated object.

The non-verification blueprint is complete and the verification reference has returned, but no verification object has
been materialized. Do not restate the prefix, start a root object, or reread any source or contract. Record compact
blueprint entries for exactly two observation policies, ten event extractors, and every fixed non-repeated rule from the
authoritative clarifications exactly once. Do not materialize those rule objects or begin any repeated rule family or
terminal path. These entries will become typed IR literals only at the final StructuredOutput boundary. End that bounded core-blueprint stage with exactly one `Read` of the actual Skill base directory joined with
`references/checkpoints/02-begin-9-1-inventory.md`. Do not call `StructuredOutput` before that next checkpoint returns.
